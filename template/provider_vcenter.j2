provider "vsphere" {
  user                 = "{{ user }}"
  password             = "{{ password }}"
  vsphere_server       = "{{ vsphere_server }}"
  alias                = "vcenter{{ alias }}"
  allow_unverified_ssl = true
}